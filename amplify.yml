version: 2.0
applications:
     -  appRoot: /rea 
        env:
            variables: []
        frontend:
            phases:
                build:
                    commands:
                        - 'echo no-op'
            artifacts:
                files:
                    - '**/*'
                baseDirectory: /
            cache:
                paths:
                    - node_modules
     -  appRoot: /other 
        env:
            variables: []
        frontend:
            phases:
                build:
                    commands:
                        - 'echo no-op'
            artifacts:
                files:
                    - '**/*'
                baseDirectory: /
            cache:
                paths:
                    - node_modules
